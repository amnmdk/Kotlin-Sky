// Ce fichier ne contient plus de code Jetpack Compose.
// La gestion du thème se fait désormais via les ressources XML (styles.xml, colors.xml, etc.)
// et les méthodes classiques d'Android (AppCompatDelegate, etc.).

// Si tu veux appliquer un thème, fais-le dans le manifeste ou dans les activités avec AppCompatDelegate.setDefaultNightMode(...) 